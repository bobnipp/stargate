export interface RequirementMarkerModel {
    id: string
    title: string
    description: string
    iconUrl: string
    latitude: number
    longitude: number
    markerType: string
    isOpen: boolean
    isActive: boolean
}